# ── Web (Vite + React) — copy to web/.env (gitignored) ────────
# Nur VITE_-Variablen werden ins Client-Bundle übernommen.
# API-Basis relativ: Dev über Vite-Proxy (/api -> :8080), Prod über nginx.

VITE_API_BASE_URL=/api/v1
VITE_APP_NAME=Prekaj-Zeiterfassung
VITE_ENV=development
